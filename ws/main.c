#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "actions.h"

int main()
    {
    int tabAscenseurs[21], etg;      //initialisation du tableau
    malloc(sizeof(tabAscenseurs));      //allocation dynamique
printf("Arret des ascenseurs");
free(tabAscenseurs);      //libération mémoire
    }
